import { ContentLayout } from "@/core/layout/content/content-layout";
import CardComponent from "../components/card";

const AirView = () => {
  return (
    <ContentLayout title="Contaminación del aire">
      <CardComponent />
    </ContentLayout>
  );
};

export default AirView;
